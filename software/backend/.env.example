AI_PROVIDER=groq
GROQ_API_KEY=your_groq_api_key_here
# Optional. When omitted, the backend uses its supported Groq default model.
GROQ_MODEL=openai/gpt-oss-20b
# Kept empty while the trained local advisor is disabled.
LOCAL_ADVISOR_URL=
